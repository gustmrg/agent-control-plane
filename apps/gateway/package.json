{
  "name": "@agent-control/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && mkdir -p dist/migrations && cp ../../migrations/*.sql dist/migrations/",
    "typecheck": "tsc -b --pretty false",
    "test": "pnpm build && node --test \"dist/test/**/*.test.js\"",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "@agent-control/contracts": "workspace:*",
    "dockerode": "^4.0.0",
    "fastify": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.0"
  }
}
